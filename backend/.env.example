XAI_API_KEY=
XAI_MODEL=grok-4
XAI_BASE_URL=https://api.x.ai/v1
INVOICE_PROCESSING_LOG_DIR=./logs
INVOICE_PROCESSING_INVOICES_DIR=./data/invoices
INVOICE_PROCESSING_DB_PATH=./data/inventory.db
RUN_LIVE_TESTS=0

# Cost per invoice processed manually — used for simulated $-saved metric.
# Default $12 reflects industry benchmark for fully-loaded AP manual
# processing (data entry, validation, approval routing, payment).
# Override for your org.
MANUAL_COST_PER_INVOICE_USD=12.0
